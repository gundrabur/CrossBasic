REM Example 5: Graphic patterns
10 GRAPHICS
20 CLS
30 COLOR 1
40 PRINT "Drawing geometric patterns..."
50 REM Checkerboard pattern
60 FOR X = 0 TO 800 STEP 40
70 FOR Y = 0 TO 600 STEP 40
80 LET SUM = (X / 40) + (Y / 40)
90 IF INT(SUM / 2) * 2 = SUM THEN COLOR 1 ELSE COLOR 0
100 RECT X, Y, 40, 40
110 NEXT Y
120 NEXT X
130 REM Spiral
140 COLOR 2
150 LET X = 400
160 LET Y = 300
170 LET ANGLE = 0
180 LET RADIUS = 1
190 FOR I = 1 TO 500
200 LET NEWX = 400 + RADIUS * COS(ANGLE)
210 LET NEWY = 300 + RADIUS * SIN(ANGLE)
220 LINE X, Y TO NEWX, NEWY
230 LET X = NEWX
240 LET Y = NEWY
250 LET ANGLE = ANGLE + 0.2
260 LET RADIUS = RADIUS + 0.5
270 NEXT I
280 END
